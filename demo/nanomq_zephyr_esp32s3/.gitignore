# Local Wi-Fi credentials / build overrides — never commit real SSIDs.
# Copy local.conf.example to local.conf and pass it to west build with
#   -- -DEXTRA_CONF_FILE=local.conf
local.conf

# Local build directories
build*/
